import 'package:flutter/material.dart';

void main() {
  runApp(
    new MaterialApp(
      title: "Chat",
      home: new Scaffold(
        appBar: new AppBar(
          title: new Text("Chat"),
        ),
      )
    )
  );
}